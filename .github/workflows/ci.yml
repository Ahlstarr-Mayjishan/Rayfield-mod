name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lua-syntax:
    name: Lua Syntax Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Check Lua Syntax
        run: |
          find . -name "*.lua" -not -path "./.git/*" -exec sh -c 'echo "Checking: $1" && luac -p "$1" || exit 1' _ {} \;

  verify-scripts:
    name: Run Verification Scripts
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Run Module Map Verification
        run: lua scripts/verify-module-map.lua

      - name: Run No Direct HttpGet Check
        run: lua scripts/verify-no-direct-httpget.lua

      - name: Run No Direct TweenCreate Check
        run: lua scripts/verify-no-direct-tweencreate.lua

  luacheck:
    name: Luacheck Ratchet
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Install LuaRocks + Luacheck
        run: |
          sudo apt-get update
          sudo apt-get install -y luarocks
          luarocks install luacheck || sudo luarocks install luacheck
          echo "$HOME/.luarocks/bin" >> "$GITHUB_PATH"
          luacheck --version

      - name: Collect changed Lua files
        id: changed_lua
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.lua' > changed-lua.txt
          else
            if [[ "${{ github.event.before }}" =~ ^0+$ ]]; then
              git ls-files '*.lua' > changed-lua.txt
            else
              git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- '*.lua' > changed-lua.txt
            fi
          fi
          echo "Changed Lua files:"
          cat changed-lua.txt || true

      - name: Run Luacheck (full scope)
        id: luacheck_run
        shell: bash
        run: |
          mkdir -p luacheck-context debt-reports
          set +e
          luacheck --config .luacheckrc --codes --ranges src feature "Main loader" tests scripts 2>&1 | tee luacheck-report.txt
          status=${PIPESTATUS[0]}
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Build Luacheck Context Artifact
        if: always()
        run: lua scripts/emit-luacheck-context.lua luacheck-report.txt luacheck-context

      - name: Enforce Luacheck Ratchet
        shell: bash
        run: |
          lua scripts/ci-luacheck-ratchet.lua luacheck-report.txt .ci/luacheck-baseline.json changed-lua.txt debt-reports/luacheck-ratchet.txt

      - name: Upload Luacheck Debt Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: luacheck-debt-report
          path: |
            luacheck-report.txt
            luacheck-context/
            debt-reports/luacheck-ratchet.txt
          retention-days: 14

  selene:
    name: Selene Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Collect changed Lua files
        id: changed_lua
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.lua' > changed-lua.txt
          else
            if [[ "${{ github.event.before }}" =~ ^0+$ ]]; then
              git ls-files '*.lua' > changed-lua.txt
            else
              git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- '*.lua' > changed-lua.txt
            fi
          fi
          grep '^src/.*\.lua$' changed-lua.txt > changed-src-lua.txt || true
          echo "Changed Lua files:"
          cat changed-lua.txt || true
          echo "Changed src Lua files:"
          cat changed-src-lua.txt || true
          count=$(wc -l < changed-src-lua.txt | tr -d ' ')
          echo "count=$count" >> "$GITHUB_OUTPUT"

      - name: Skip Selene (no src lua changes)
        if: steps.changed_lua.outputs.count == '0'
        run: echo "No src/*.lua changes detected; skipping Selene."

      - name: Setup Rust
        if: steps.changed_lua.outputs.count != '0'
        uses: dtolnay/rust-toolchain@stable

      - name: Install Selene
        if: steps.changed_lua.outputs.count != '0'
        run: cargo install selene --locked

      - name: Run Selene
        if: steps.changed_lua.outputs.count != '0'
        shell: bash
        run: |
          mapfile -t files < changed-src-lua.txt
          selene "${files[@]}"

  complexity:
    name: Complexity Ratchet
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Lizard
        run: python -m pip install lizard

      - name: Collect changed Lua files
        id: changed_lua
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.lua' > changed-lua.txt
          else
            if [[ "${{ github.event.before }}" =~ ^0+$ ]]; then
              git ls-files '*.lua' > changed-lua.txt
            else
              git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- '*.lua' > changed-lua.txt
            fi
          fi
          echo "Changed Lua files:"
          cat changed-lua.txt || true

      - name: Generate lizard reports
        shell: bash
        run: |
          mkdir -p debt-reports
          python -m lizard src feature "Main loader" tests scripts -l lua -X > lizard-report.xml
          python -m lizard src feature "Main loader" tests scripts -l lua -C 30 -L 220 > lizard-report.txt || true
          cat lizard-report.txt

      - name: Enforce complexity ratchet
        shell: bash
        run: |
          lua scripts/ci-complexity-ratchet.lua lizard-report.xml .ci/complexity-baseline.json changed-lua.txt debt-reports/complexity-ratchet.txt

      - name: Upload complexity debt artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: complexity-debt-report
          path: |
            lizard-report.xml
            lizard-report.txt
            debt-reports/complexity-ratchet.txt
          retention-days: 14

  global-access:
    name: Global Access Ratchet
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Collect changed Lua files
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.lua' > changed-lua.txt
          else
            if [[ "${{ github.event.before }}" =~ ^0+$ ]]; then
              git ls-files '*.lua' > changed-lua.txt
            else
              git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- '*.lua' > changed-lua.txt
            fi
          fi
          echo "Changed Lua files:"
          cat changed-lua.txt || true

      - name: Enforce _G access ratchet
        shell: bash
        run: |
          mkdir -p debt-reports
          lua scripts/ci-global-access-ratchet.lua .ci/global-access-baseline.json changed-lua.txt debt-reports/global-access-ratchet.txt

      - name: Upload global access debt artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: global-access-debt-report
          path: debt-reports/global-access-ratchet.txt
          retention-days: 14

  format-check:
    name: StyLua Format Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Collect changed Lua files
        id: changed_lua
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- '*.lua' > changed-lua.txt
          else
            if [[ "${{ github.event.before }}" =~ ^0+$ ]]; then
              git ls-files '*.lua' > changed-lua.txt
            else
              git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" -- '*.lua' > changed-lua.txt
            fi
          fi
          echo "Changed Lua files:"
          cat changed-lua.txt || true
          count=$(wc -l < changed-lua.txt | tr -d ' ')
          echo "count=$count" >> "$GITHUB_OUTPUT"

      - name: Skip StyLua (no lua changes)
        if: steps.changed_lua.outputs.count == '0'
        run: echo "No *.lua changes detected; skipping StyLua format check."

      - name: Setup Rust
        if: steps.changed_lua.outputs.count != '0'
        uses: dtolnay/rust-toolchain@stable

      - name: Install StyLua
        if: steps.changed_lua.outputs.count != '0'
        run: cargo install stylua --locked

      - name: Run StyLua Check
        if: steps.changed_lua.outputs.count != '0'
        shell: bash
        run: |
          mapfile -t files < changed-lua.txt
          stylua --check "${files[@]}"

  auto-format-on-push:
    name: Auto Format on Push
    if: github.event_name == 'push' && vars.AUTO_FORMAT_ON_PUSH == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install StyLua
        run: cargo install stylua --locked

      - name: Apply StyLua
        run: stylua .

      - name: Commit formatted files
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(ci): auto-format lua files with stylua"
          file_pattern: "**/*.lua"

  bundle-build:
    name: Build Production Bundle
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [lua-syntax, verify-scripts, luacheck, selene, complexity, global-access, format-check]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: "5.4"

      - name: Build Bundle
        run: lua scripts/build-bundle.lua

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-bundle
          path: dist/
          retention-days: 7
